name: Test

on:
  push:

jobs:
  build:
    runs-on: macos-13
    steps:
      - uses: actions/checkout@v3
      - name: Use Xcode 15.0
        run: xcode-select --switch /Applications/Xcode_15.0.app
      - name: Build
        run: swift build
      - name: Test
        run: swift test -v
